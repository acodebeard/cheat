/* cheats.config.js
   Toggle cheats here — no edits needed in the main logic file.
*/
window.CHEAT_CONFIG = {
  enabled: {
    confidence: true,
    geocities: true,
    konami: true,
    drunk: true,
  },

  // Optional: global behavior
  maxGapMs: 2000
};
